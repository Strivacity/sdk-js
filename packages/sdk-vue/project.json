{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "sdk-vue",
  "projectType": "library",
  "sourceRoot": "packages/sdk-vue/src",
  "targets": {
    "type-check": {
      "executor": "nx:run-commands",
      "dependsOn": ["sdk-core:build"],
      "options": {
        "command": "vue-tsc --noEmit --project packages/sdk-vue/tsconfig.app.json"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["type-check"],
      "outputs": ["{workspaceRoot}/packages/sdk-vue/dist"],
      "defaultConfiguration": "development",
      "options": {
        "cwd": "packages/sdk-vue"
      },
      "configurations": {
        "serve": {
          "mode": "development",
          "command": "vite build --watch"
        },
        "development": {
          "mode": "development",
          "command": "vite build"
        },
        "production": {
          "mode": "production",
          "command": "vite build"
        }
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "cwd": "packages/sdk-vue",
        "command": "vitest"
      }
    }
  }
}
