{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "sdk-preact",
  "projectType": "library",
  "sourceRoot": "packages/sdk-preact/src",
  "targets": {
    "type-check": {
      "executor": "nx:run-commands",
      "dependsOn": ["sdk-core:build"],
      "options": {
        "command": "tsc --noEmit --project packages/sdk-preact/tsconfig.app.json"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["type-check"],
      "outputs": ["{workspaceRoot}/packages/sdk-preact/dist"],
      "defaultConfiguration": "development",
      "options": {
        "cwd": "packages/sdk-preact"
      },
      "configurations": {
        "serve": {
          "mode": "development",
          "command": "vite build --watch"
        },
        "development": {
          "mode": "development",
          "command": "vite build"
        },
        "production": {
          "mode": "production",
          "command": "vite build"
        }
      }
    }
  }
}
